# =============================================================================
# CATERING PRO - ENVIRONMENT VARIABLES
# Bu dosyayı .env olarak kopyalayıp değerleri doldurun
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Zorunlu)
# -----------------------------------------------------------------------------
# Supabase Connection String
# Format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/catering_db

# Supabase Service Key (Storage ve admin işlemleri için)
# Project Settings → API → service_role key
SUPABASE_SERVICE_KEY=your-supabase-service-key-here
SUPABASE_URL=https://your-project.supabase.co

# -----------------------------------------------------------------------------
# AUTHENTICATION (Zorunlu)
# -----------------------------------------------------------------------------
# JWT için secret key (en az 32 karakter, random string)
JWT_SECRET=your-jwt-secret-minimum-32-characters-here
JWT_EXPIRES_IN=7d

# NextAuth için secret (Frontend - artık kullanılmıyor, custom AuthContext kullanılıyor)
# NEXTAUTH_SECRET=your-nextauth-secret-change-this

# -----------------------------------------------------------------------------
# AI SERVİSLERİ (Opsiyonel - AI özellikleri için gerekli)
# -----------------------------------------------------------------------------
# Google Gemini API (Döküman analizi için)
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-2.0-flash-exp

# Anthropic Claude API (AI Asistan için)
# https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# SCRAPER (Opsiyonel - İhale scraping için)
# -----------------------------------------------------------------------------
# ihalebul.com hesap bilgileri
IHALEBUL_USERNAME=your_username
IHALEBUL_PASSWORD=your_password

# -----------------------------------------------------------------------------
# UYUMSOFT ENTEGRASYONU (Opsiyonel - Fatura sync için)
# -----------------------------------------------------------------
UYUMSOFT_API_URL=https://efatura.uyumsoft.com.tr/api
UYUMSOFT_USERNAME=your_username
UYUMSOFT_PASSWORD=your_password

# -----------------------------------------------------------------------------
# SERVER AYARLARI (Zorunlu Production'da)
# -----------------------------------------------------------------------------
# Production'da mutlaka "production" olmalı
NODE_ENV=development
PORT=3001
API_PORT=3001

# Proje dizin yolları (Opsiyonel - varsayılan olarak otomatik tespit edilir)
# PROJECT_ROOT=/path/to/catering
# WHATSAPP_SERVICE_PATH=/path/to/catering/services/whatsapp

# -----------------------------------------------------------------------------
# FRONTEND ENV (frontend/.env.local)
# -----------------------------------------------------------------------------
# Supabase Public Keys (Güvenli - client-side kullanılabilir)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API URL (Opsiyonel - varsayılan localhost:3001)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# NOTLAR
# -----------------------------------------------------------------------------
# 1. Bu dosya .gitignore'da olmalı - asla commit etmeyin!
# 2. Production'da NODE_ENV=production olmalı
# 3. Frontend için NEXT_PUBLIC_ prefix'li değişkenleri frontend/.env.local'a kopyalayın
# 4. Supabase connection string'i Project Settings → Database → Connection string'den alın
# 5. SUPABASE_SERVICE_KEY sadece backend'de kullanılmalı (güvenlik)
